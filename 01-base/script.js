(function() {
    
    
        do{
            var nom = prompt("nom?");
            var age = prompt("age?");
            var ville =prompt("ville ? ");
            var validation = prompt(nom+" "+age+" "+ville);
        }while(validation==="oui");

    })();